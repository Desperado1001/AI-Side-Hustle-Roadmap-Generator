# 项目实践计划（基于 README 需求）

## 目标与范围
- **目标**：在 6–7 周内完成 AI Side‑Hustle Roadmap Generator 的 MVP，从需求验证到软发布。  
- **范围**：聚焦核心功能——收集用户输入并生成 6–12 个月的个性化副业路线图；不包含多语言、协作、模板市场等延伸功能。

## 里程碑与时间表

### 第 1 周：问题验证与需求确认
- 用户访谈/问卷（10–20 份）验证副业路线图需求。
- 明确 MVP 用户画像与使用场景（独立开发者、自由职业者）。
- 输出：需求清单、核心价值主张、成功指标（如 70% 试用用户完成问卷）。

### 第 2 周：最小功能集与数据准备
- 确定 MVP 功能边界：
  - 问卷收集（技能、兴趣、时间、预算、收入目标）。
  - 路线图生成与展示。
  - 保存/导出为 PDF（可选，视工作量）。
- 设计 LLM Prompt 模板与样例输入/输出。
- 输出：PRD、Prompt 模板草案、示例路线图样本。

### 第 3 周：前端原型与页面开发
- Figma 快速原型（登录、问卷、结果页、仪表盘）。
- 使用 Next.js + Tailwind + shadcn/ui 实现核心页面：
  - 登录/注册页（可先跳过真实认证，用本地 mock）。
  - 问卷页。
  - 结果页（展示推荐项目、MVP 功能、营销、时间线、预估收益）。
- 输出：前端可点击原型 + 基础页面。

### 第 4 周：后端 API 与模型集成
- 搭建 API（Next.js API Routes 或 FastAPI）：
  - `POST /api/roadmap` 生成路线图。
  - `GET /api/roadmap/:id` 查询历史路线图。
- 接入 LLM（OpenAI/Claude/Gemini），串联 Prompt 逻辑。
- 输出：API 可用、可生成路线图的 Demo。

### 第 5 周：数据存储与账户功能
- 接入数据库（PostgreSQL/Supabase）：
  - User/Roadmap/Subscription 表。
- 实现基础登录/鉴权（Supabase Auth 或 NextAuth）。
- 路线图保存与历史列表。
- 输出：支持登录+保存+查看历史。

### 第 6 周：测试与软发布
- 内部测试（5–10 位测试用户）。
- 修复主要问题并优化 Prompt 输出质量。
- 部署到 Vercel（或 Render/Railway）。
- 输出：可公开访问的 MVP。

### 第 7 周及以后：持续迭代
- 优化 UI/UX、补充营销策略模板。
- 增加订阅支付（Stripe/支付宝/微信）。
- 引入模板市场或高级功能。

## 关键交付物清单
- 需求文档（PRD）
- Prompt 模板与示例输出
- 前端页面（问卷 + 结果 + 仪表盘）
- 后端 API（roadmap 生成与查询）
- 数据库模型与认证
- MVP 部署地址

## 风险与应对
- **LLM 输出不稳定**：增加 prompt 示例、规范输出格式；提供“重生成”按钮。
- **开发周期延误**：聚焦核心功能，非必要功能延后。
- **数据质量不足**：先用人工样例补充训练与示例。

## 成功指标（MVP）
- 70% 用户完成问卷并获得路线图。
- 30% 试用用户愿意保存/导出路线图。
- 10% 用户表达付费意愿或加入等候名单。
